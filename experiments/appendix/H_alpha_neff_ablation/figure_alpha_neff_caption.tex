% Figure: 3D alpha x n_eff x gamma ablation heatmap

\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{H_alpha_neff_ablation/results/alpha_neff_gamma_grid_figure.png}
\caption{%
    \textbf{Joint $\alpha \times \neff \times \gamma$ ablation}
    (top row: Pareto AUC, bottom row: mean reward at $\lambda{=}0$;
    one column per forgetting factor~$\gamma$).
    Each cell shows the metric averaged over 20~seeds.
    Starred (\ding{72}) cells mark the global optimum selected by
    Pareto AUC --- the area under the cost--reward Pareto frontier
    traced by sweeping~$\lambda$.
    The Pareto AUC criterion favours hyperparameters that produce
    diverse, intelligent routing across the full cost spectrum,
    rather than a static ``always pick the strongest model'' policy
    that emerges from quality-only ($\lambda{=}0$) tuning.
}
\label{fig:alpha_neff_grid}
\end{figure}
