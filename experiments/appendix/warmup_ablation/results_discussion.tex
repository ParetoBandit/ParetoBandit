% Appendix: Cold-Start vs Warmup Prior Regret

\subsection{Cold-Start vs Warmup Priors (Figure~\ref{fig:warmup_ablation})}
\label{sec:warmup_ablation}

We isolate the contribution of warmup priors by comparing
ParetoBandit with offline priors against a tabula-rasa cold start
(identity covariance, zero bias) on the $K{=}3$ stationary portfolio.
Each condition uses the optimal hyperparameters from the Experiment~04
sweep: ParetoBandit uses $\alpha{=}0.1$, $n_\mathit{eff}{=}1{,}000$,
$\gamma{=}0.995$; Tabula Rasa uses $\alpha{=}0.1$,
$n_\mathit{eff}{=}1$, $\gamma{=}0.995$ (disjoint LinUCB).

\begin{center}
\small
\begin{tabular}{@{}lrrrr@{}}
\toprule
\textbf{Condition} & \textbf{Regret} (95\% CI) & \textbf{Regret@200} (95\% CI) & \textbf{Mean Rwd} & \textbf{Oracle Agr.} \\
\midrule
ParetoBandit (warmup) & $\mathbf{72.0}$\,[70.7, 73.2] & $\mathbf{5.9}$\,[5.6, 6.3]  & $\mathbf{0.923}$ & $\mathbf{0.642}$ \\
Tabula Rasa        & $79.4$\,[77.4, 81.5]         & $10.3$\,[8.6, 12.0]         & $0.919$           & $0.632$           \\
Random             & $145.3$\,[142.9, 147.7]      & $16.3$\,[15.5, 17.1]        & $0.882$           & $0.528$           \\
\bottomrule
\end{tabular}
\end{center}

\noindent
Both conditions use their respective optimal hyperparameters from
Experiment~04, giving Tabula~Rasa its best possible configuration.
Even so, warmup priors reduce total regret by 9\% ($\Delta{=}-7.5$,
paired $t$-test $p < 10^{-4}$) and early regret (step~200) by
43\%.  The advantage is concentrated in the first ${\sim}200$
steps (Figure~\ref{fig:warmup_ablation}), where the warmup router
begins with informed arm-quality estimates and avoids the
exploratory phase that the cold-start router must perform.
Beyond step~${\sim}400$, the two routers converge to comparable
per-step quality, but the regret gap established early persists
through the full horizon.
Twenty seeds; 95\% bootstrap CI shading; val split ($n{=}1{,}785$).

\paragraph{Uncertainty evolution (Figure~\ref{fig:warmup_uncertainty}).}
To visualise the mechanism behind this regret gap, Figure~\ref{fig:warmup_uncertainty} plots $\mathrm{tr}(A_a^{-1})$---the total uncertainty per arm---for both conditions across the trajectory.
Warmup-initialised arms (solid lines) start at $\mathrm{tr}(A_a^{-1}) \approx 0.72$, reflecting the $n_\mathit{eff}{=}1{,}000$ pseudo-observations encoded in the offline prior.
Cold-started arms (dashed lines) begin at $\mathrm{tr}(A_a^{-1}) = d / \lambda = 26$---a ${\sim}36{\times}$ uncertainty gap.
Because the UCB exploration bonus scales as $\alpha\sqrt{x^\top A_a^{-1} x}$, this gap means the cold-start router must spend hundreds of steps exploring before it accumulates enough information to route as confidently as the warmup router does from step~1.
With geometric forgetting ($\gamma{=}0.995$), arms that receive few observations see their uncertainty \emph{grow} over time as the prior decays; the warmup Llama-8B trace rises from 0.72 to ${\sim}10$, illustrating how forgetting trades long-term certainty for adaptability.
The two conditions largely converge by step~${\sim}400$ for frequently selected arms (Gemini-Pro, Mistral-Large), consistent with the regret convergence in Figure~\ref{fig:warmup_ablation}.
