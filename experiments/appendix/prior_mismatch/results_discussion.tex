% Appendix: Prior Mismatch Sensitivity Analysis

\section{Prior Mismatch Sensitivity}
\label{appendix:prior_mismatch}

The warmup ablation (Appendix~\ref{sec:warmup_ablation}) demonstrated
that well-calibrated priors substantially reduce regret and eliminate
catastrophic cold-start failures under stationary conditions.
All experiments in this paper use priors trained on a representative
training set drawn from the same distribution as the test traffic.
In practice, however, prior quality degrades after deployment:
the prompt distribution shifts as users adopt new workflows,
model providers silently update weights, and the roster itself
changes as models are added or retired.
A practitioner deploying warmup priors needs to know
\emph{how wrong} the prior can be before cold start becomes the
safer option, and whether a conservative prior strength can
limit the damage when calibration quality is uncertain.

\paragraph{Why not re-optimise?}
The production hyperparameters (Appendix~\ref{sec:hparam_sweep})
are tuned once on a representative training set via the
$T_\text{adapt}$-constrained Pareto-knee procedure, which
couples $n_\mathit{eff}$ and $\gamma$ through the adaptation horizon.
The selected prior strength ($n_\mathit{eff} \approx 1{,}164$,
Eq.~\ref{eq:prior_scale}) implicitly assumes the prior is
directionally correct: high $n_\mathit{eff}$ tells the bandit to
trust the offline beliefs for many observation-equivalents before
online evidence takes over.
If the prior is wrong, high $n_\mathit{eff}$ amplifies the harm by
delaying the point at which online data overrides the prior.
Re-running the Pareto-knee sweep for every possible prior-quality
level is impractical in production, so this experiment instead
asks whether a single tunable parameter---$n_\mathit{eff}$---can
serve as a safety knob that limits damage from degraded priors
while preserving the variance-reduction benefit for good ones.

\paragraph{Design: mismatch gradient.}
We construct five prior-quality levels forming a mismatch gradient,
ordered from ideal to adversarial:
\begin{enumerate}[nosep,leftmargin=*]
  \item \textbf{Well-calibrated:} priors from the full training set
    ($n{=}\prmWellCalNPrompts$)---the shipped default.
  \item \textbf{Random-1680:} a random subsample of \prmRandNPrompts{} training
    prompts, matching the GSM8K-only count while preserving the full
    distribution.  This is a \emph{sample-size control}: any gap
    between Random-1680 and Well-calibrated isolates covariance
    estimation noise from domain effects.
  \item \textbf{MMLU-only:} priors from the \prmMMLUNPrompts{} MMLU prompts only.
    The prior preserves the correct model ranking (Gemini~$>$~Mistral~$\gg$~Llama)
    but with knowledge-domain-specific magnitudes.
  \item \textbf{GSM8K-only:} priors from the \prmGSMNPrompts{} GSM8K (math) prompts only.
    All models score ${\sim}\prmGSMMinBiasPred{+}$ on math, so the prior encodes
    near-zero arm differentiation.
  \item \textbf{Inverted:} priors from the full training set with
    Llama and Gemini rewards swapped, so the prior believes the
    cheapest model is best and vice versa.
\end{enumerate}
These levels represent failure modes that arise naturally in
deployment: (1--2)~an operator who collected representative data;
(3)~an operator who collected data from a single knowledge domain;
(4)~an operator whose data happens to come from a task where all
models perform similarly; and (5)~the worst case, where an update
inverts the relative model ranking (e.g., because a provider
silently degrades a model that was previously the best).

\paragraph{Design: $n_\mathit{eff}$ as a safety knob.}
Each quality level is tested at \prmNNeff{} $n_\mathit{eff}$ values
(10, 100, 1000), spanning three orders of magnitude.
These values bracket a key trade-off.
Low $n_\mathit{eff}$ (${\approx}10$) treats the prior as a gentle
nudge: the bandit overrides it within tens of observations,
limiting the damage from a bad prior but also limiting the
cold-start benefit from a good one.
High $n_\mathit{eff}$ (${\approx}1{,}000$) trusts the prior for
hundreds of effective observations: beneficial when the prior is
correct, but potentially harmful when it confidently encodes the
wrong ranking.
Together with the quality gradient, the
$\prmNQualities \times \prmNNeff$ grid reveals whether there exists
an $n_\mathit{eff}$ regime where any prior---even a domain-mismatched
one---helps more than it hurts.

\paragraph{Design: baseline.}
The no-prior baseline is the independently optimised Tabula Rasa
($\alpha{=}\waTRAlpha$, $\gamma{=}\prmTRGamma$).
As established in the warmup ablation
(Appendix~\ref{sec:warmup_ablation}), the Pareto-knee sweep selected
the same $\gamma{=}\prmTRGamma$ for both warmup and Tabula Rasa, so
both share the same effective memory (${\sim}\prmEffMemWarmup$ steps).
This yields $\prmNQualities \times \prmNNeff + 1 = \prmNConditions$ conditions.
All warmup conditions use $\alpha{=}\prmAlpha$, $\gamma{=}\prmGamma$ (disjoint
LinUCB).  The warmup hyperparameters were \emph{not} re-optimised per
prior-quality level---matching the production scenario where prior
quality degrades after deployment without hyperparameter adjustment.

\paragraph{Design: unconstrained regime.}
Evaluation follows the same cumulative-regret protocol as the warmup
ablation: held-out test split ($n{=}\prmNPrompts$), \prmNseeds{} seeds
(offset${=}\prmSeedOffset$, aligned for paired comparisons), unconstrained
(no budget pacer).
We deliberately restrict this experiment to the unconstrained regime
for two reasons.
First, the budget pacer constrains arm selection, which limits the
online observations the bandit receives to correct a bad prior;
budget-induced failures and prior-induced failures would be
confounded.
Second, the warmup ablation (Appendix~\ref{sec:warmup_ablation})
already characterises the pacer interaction for well-calibrated
priors vs.\ tabula rasa.
Adding budget constraints here would conflate two failure modes
without additional insight, since the prior-quality threshold for
harm is determined by the bandit's learning dynamics, not the pacer.

\paragraph{Statistical methodology.}
We use the same paired-comparison protocol as
Appendix~\ref{sec:warmup_ablation}: exact binomial sign test for
location shift, Fisher exact test on a $2{\times}2$
catastrophic-failure table for tail risk, with Holm--Bonferroni
correction across all \prmNPairwise{} pairwise comparisons per baseline.
All confidence intervals are \prmCILevel\% percentile-bootstrap
(\prmBootstrapResamples{} resamples, seed-level resampling);
median CIs resample the median directly rather than relying on
normality, which is inappropriate for the heavy-tailed baseline
distributions observed here.
A seed is classified as catastrophic if its regret exceeds
$\prmCatMultiplier{\times}$ the Tabula Rasa median.

\paragraph{Results.}
Figure~\ref{fig:prior_mismatch_heatmap} summarises total regret
across the quality--strength grid.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{../experiments/appendix/prior_mismatch/results/prior_mismatch_heatmap.png}
\caption{Total cumulative regret for ParetoBandit across the prior-quality vs.\ prior-strength grid (\prmNseeds~seeds, unconstrained, test split).  All conditions use the same ParetoBandit router; only the warmup prior varies.  Tabula Rasa is ParetoBandit without priors.  \textbf{Bold} cells exceed the Tabula Rasa median; normal-weight cells fall below it.}
\label{fig:prior_mismatch_heatmap}
\end{figure*}

\emph{Directionally correct priors help at every strength.}
Well-calibrated priors reduce median total regret monotonically
with $n_\mathit{eff}$, relative to the Tabula Rasa baseline
(median $\prmCatRefMedian$\,[$\prmTRRegretMedianCILo$, $\prmTRRegretMedianCIHi$]):
\begin{itemize}[nosep,leftmargin=*]
  \item $n_\mathit{eff}{=}10$: median
    $\prmWellCalTenRegretMedian$\,[$\prmWellCalTenRegretMedianCILo$, $\prmWellCalTenRegretMedianCIHi$]
    ($\prmWellCalTenReductionPct\%$\,[$\prmWellCalTenReductionPctCILo$, $\prmWellCalTenReductionPctCIHi$] reduction).
  \item $n_\mathit{eff}{=}100$: median
    $\prmWellCalHundredRegretMedian$\,[$\prmWellCalHundredRegretMedianCILo$, $\prmWellCalHundredRegretMedianCIHi$]
    ($\prmWellCalHundredReductionPct\%$\,[$\prmWellCalHundredReductionPctCILo$, $\prmWellCalHundredReductionPctCIHi$] reduction).
  \item $n_\mathit{eff}{=}1{,}000$: median
    $\prmWellCalThousandRegretMedian$\,[$\prmWellCalThousandRegretMedianCILo$, $\prmWellCalThousandRegretMedianCIHi$]
    ($\prmWellCalThousandReductionPct\%$\,[$\prmWellCalThousandReductionPctCILo$, $\prmWellCalThousandReductionPctCIHi$] reduction).
\end{itemize}
At $n_\mathit{eff}{=}1{,}000$, Well-calibrated wins
\prmTestWellCalThousandCondWins{} of \prmNseeds{} seeds
(Holm-corrected sign $p{=}\prmTestWellCalThousandSignPHolm$);
Random-1680 wins \prmTestRandThousandCondWins{} of \prmNseeds{}
(Holm $p{=}\prmTestRandThousandSignPHolm$).
At lower $n_\mathit{eff}$ values, the per-seed improvements are
directionally consistent but do not survive Holm correction with
\prmNseeds~seeds.

\emph{Sample size does not matter---distributional match does.}
A practitioner might worry that the Well-calibrated prior benefits
from a larger training set rather than better calibration.
The Random-1680 control rules this out: it uses only $\prmRandNPrompts$
prompts (matching the GSM8K-only count) drawn from the full
distribution and performs comparably to Well-calibrated at every
$n_\mathit{eff}$ (e.g., median
$\prmRandThousandRegretMedian$\,[$\prmRandThousandRegretMedianCILo$, $\prmRandThousandRegretMedianCIHi$]
vs.\ $\prmWellCalThousandRegretMedian$\,[$\prmWellCalThousandRegretMedianCILo$, $\prmWellCalThousandRegretMedianCIHi$]
at $n_\mathit{eff}{=}1{,}000$).
This confirms that covariance estimation saturates
quickly---$\prmRandNPrompts$ representative prompts are sufficient for
$d{=}\prmFeatureDim$ features---and that the domain-mismatch findings
below are not confounded by sample-count effects.
The practical implication is that operators need a \emph{representative}
training sample, not a large one.

\emph{Domain-mismatched priors still beat cold start.}
Even when the prior is trained on a single domain rather than
representative traffic, median regret stays below the Tabula Rasa
baseline at every $n_\mathit{eff}$---the prior never hurts.
MMLU-only priors reduce median regret to
$\prmMMLUHundredRegretMedian$\,[$\prmMMLUHundredRegretMedianCILo$, $\prmMMLUHundredRegretMedianCIHi$]
at $n_\mathit{eff}{=}100$ and
$\prmMMLUThousandRegretMedian$\,[$\prmMMLUThousandRegretMedianCILo$, $\prmMMLUThousandRegretMedianCIHi$]
at $1{,}000$
(vs.\ Tabula Rasa $\prmCatRefMedian$\,[$\prmTRRegretMedianCILo$, $\prmTRRegretMedianCIHi$]),
despite being trained on a single knowledge domain.
GSM8K-only priors---which encode near-zero arm differentiation---lower
median regret to
$\prmGSMTenRegretMedian$\,[$\prmGSMTenRegretMedianCILo$, $\prmGSMTenRegretMedianCIHi$]
through
$\prmGSMThousandRegretMedian$\,[$\prmGSMThousandRegretMedianCILo$, $\prmGSMThousandRegretMedianCIHi$]
across $n_\mathit{eff}$ values.
After Holm correction, these reductions are directionally consistent
but individually non-significant at \prmNseeds~seeds
(Holm-corrected $p \geq \prmDomainMinPHolm$ for all domain-mismatched conditions).
Crucially, no domain-mismatched prior \emph{increases} median regret
at any $n_\mathit{eff}$: partial knowledge is consistently at least as good
as no knowledge.

\emph{Inverted priors cause harm that scales with $n_\mathit{eff}$.}
This is the only condition where priors hurt.
When the prior confidently encodes the wrong model ranking
(cheapest = best), the router initially routes traffic to the worst
model and must accumulate online evidence to override the prior.
Higher $n_\mathit{eff}$ delays that override, so damage grows with
prior strength (vs.\ Tabula Rasa median
$\prmCatRefMedian$\,[$\prmTRRegretMedianCILo$, $\prmTRRegretMedianCIHi$]):
\begin{itemize}[nosep,leftmargin=*]
  \item $n_\mathit{eff}{=}10$: median
    $\prmInvTenRegretMedian$\,[$\prmInvTenRegretMedianCILo$, $\prmInvTenRegretMedianCIHi$]
    (modest; the prior is overridden quickly).
  \item $n_\mathit{eff}{=}100$: median
    $\prmInvHundredRegretMedian$\,[$\prmInvHundredRegretMedianCILo$, $\prmInvHundredRegretMedianCIHi$].
  \item $n_\mathit{eff}{=}1{,}000$: median
    $\prmInvThousandRegretMedian$\,[$\prmInvThousandRegretMedianCILo$, $\prmInvThousandRegretMedianCIHi$]---a
    $\prmInvThousandIncreasePct\%$ increase over the baseline
    ($+\prmInvThousandExcessRegret$~regret
    units\,[$\prmInvThousandExcessRegretCILo$, $\prmInvThousandExcessRegretCIHi$]).
\end{itemize}
\noindent
Tabula Rasa wins \prmTestInvThousandBaseWins{} of \prmNseeds{} seeds
at $n_\mathit{eff}{=}1{,}000$
(Holm $p{=}\prmTestInvThousandSignPHolm$, not significant at
\prmNseeds~seeds), but the practical magnitude is operationally
meaningful.

\paragraph{Reliability: the more important finding.}
For a practitioner, run-to-run consistency matters as much as average
regret.
A router that usually improves but occasionally catastrophically
misroutes is harder to trust than one that reliably delivers moderate
gains.
Figure~\ref{fig:prior_mismatch_distribution} shows that warmup priors
deliver exactly this consistency: \emph{every} warmup condition---including
domain-mismatched and even inverted priors---produces a tight,
unimodal per-seed regret distribution, while Tabula Rasa exhibits a
wide spread ($\mathit{std}{=}\prmTRRegretStd$).
Concretely, non-inverted warmup conditions have
$\mathit{std} \leq \prmNonInvStdMax$ (range:
$\prmNonInvStdMin$--$\prmNonInvStdMax$); even the worst condition
(Inverted at $n_\mathit{eff}{=}10$) achieves
$\mathit{std}{=}\prmInvTenRegretStd$---still far tighter than
Tabula Rasa's $\prmTRRegretStd$.
All \prmNPairwise{} warmup conditions record zero catastrophic
failures (regret $> \prmCatMultiplier{\times}$ the Tabula Rasa median,
i.e., $> \prmCatThreshold$).

This reliability benefit is arguably more deployment-relevant
than the regret reduction.
The warmup ablation (Appendix~\ref{sec:warmup_ablation}) shows that
under budget constraints, Tabula Rasa's heavy tail becomes
catastrophic: \prmWaBudgetCatPct\% of seeds fail at moderate and
loose budgets ($p_\text{Fisher}{=}\prmWaBudgetFisherP$), while
warmup priors eliminate this failure mode entirely.
The present experiment extends that finding: the reliability
benefit is \emph{robust to prior degradation}.
Operators who can only collect \prmRandNPrompts{} prompts from a roughly
representative distribution, or who need to reuse priors from a
related domain, retain the same variance reduction as those
with a perfectly calibrated \prmWellCalNPrompts-prompt training set.

\paragraph{Practical implications.}
The prior-quality threshold for harm is high: priors need to be
\emph{actively wrong} (inverting model rankings) before they degrade
performance.  The reliability benefit---tight, unimodal distributions
vs.\ Tabula Rasa's heavy tail---is robust across the entire quality
gradient.
The $n_\mathit{eff}$ parameter serves as a safety knob:
$1{,}000$ for representative priors
($\prmWellCalThousandReductionPct\%$\,[$\prmWellCalThousandReductionPctCILo$, $\prmWellCalThousandReductionPctCIHi$]
regret reduction), $100$ as a safe default for domain-shifted priors
($\prmWellCalHundredReductionPct\%$\,[$\prmWellCalHundredReductionPctCILo$, $\prmWellCalHundredReductionPctCIHi$]
reduction), and $10$ when quality is
entirely uncertain (worst-case damage limited to ${\sim}\prmInvTenDamagePct\%$, while
non-adversarial priors still reduce regret by $\prmGSMTenReductionPct$--$\prmRandTenReductionPct\%$).
Tabula rasa is appropriate only when the prior is known to be
systematically inverted.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{../experiments/appendix/prior_mismatch/results/prior_mismatch_distribution.png}
\caption{Per-seed regret distributions across prior-quality levels at $n_\mathit{eff}{=}1{,}000$ (\prmNseeds~seeds, unconstrained).  Tabula Rasa exhibits a wide distribution ($\mathit{std}{=}\prmTRRegretStd$), while all warmup conditions---including domain-mismatched and inverted priors---produce tight, unimodal distributions ($\mathit{std} \leq \prmGSMThousandRegretStd$) with zero catastrophic failures.}
\label{fig:prior_mismatch_distribution}
\end{figure*}
