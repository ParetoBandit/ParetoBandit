\begin{figure*}[htbp]
    \centering
    \includegraphics[width=\linewidth]{../experiments/03_budget_plus_drift/results/adaptation_dynamics.pdf}
    \caption{BanditGPT dual-variable dynamics under a mid-stream
    Gemini price drop (step~912; $K{=}3$, 50~seeds, 95\% bootstrap CI).
    %
    \textbf{(a)}~The dual variable $\lambda_t$ reveals qualitatively
    distinct responses: at tight budget, $\lambda_t$ \emph{rises}
    (binding constraint); at moderate/loose budgets, $\lambda_t$
    decays to zero (slack constraint).
    %
    \textbf{(b)}~Gemini selection fraction mirrors the dual
    signal---tight budget drives $86\%$ Gemini adoption;
    moderate/loose converge toward the unconstrained baseline
    (dash-dot).
    %
    \textbf{(c)}~Running average cost per request; dotted lines
    mark budget targets.
    All three regimes exhibit the correct Lagrangian response.
    See Table~\ref{tab:budget_compliance} for quantitative
    compliance comparisons with baselines.
    }
    \label{fig:exp03_adaptation}
\end{figure*}
