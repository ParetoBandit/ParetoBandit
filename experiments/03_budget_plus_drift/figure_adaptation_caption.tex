\begin{figure*}[htbp]
    \centering
    \includegraphics[width=\linewidth]{../experiments/03_budget_plus_drift/results/adaptation_dynamics.pdf}
    \caption{ParetoBandit adaptation dynamics across a three-phase cost
    scenario: normal pricing $\to$ Gemini price drop (step~609)
    $\to$ price restored (step~1217).
    $K{=}3$, 50~seeds, 95\% bootstrap CI.
    %
    \textbf{(a)}~The dual variable $\lambda_t$ decays in Phase~2 as
    costs fall below target, then rises back toward Phase~1 levels in
    Phase~3 when original pricing is restored---demonstrating
    round-trip adaptation.
    %
    \textbf{(b)}~Gemini selection fraction surges in Phase~2
    (tight budget reaches ${\sim}72\%$) and returns to near-zero in
    Phase~3, mirroring the $\lambda_t$ signal.
    %
    \textbf{(c)}~Windowed average cost per request (trailing
    50~steps); dotted lines mark budget targets.
    All three regimes recover budget compliance in Phase~3.
    See Table~\ref{tab:budget_compliance} for quantitative
    compliance comparisons with baselines.
    }
    \label{fig:exp03_adaptation}
\end{figure*}
