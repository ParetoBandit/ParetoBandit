\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{figures/figure4_warmup_ablation.png}
    \caption{The value of warmup priors for $K{=}3$ routing.
    %
    \textbf{(a)}~Dev-selected deployable Pareto frontiers for
    \emph{BanditGPT} (Corralling + warmup priors; solid blue) vs.\
    \emph{Tabula rasa} (single LinUCB, no priors, no Corralling; dashed
    orange).  The cost penalty~$\lambda$ is swept over 33 values;
    error bars show 95\% $t$-CIs across 20 seeds.  Supervised baselines
    (KNN, SVM, MLP) trained via scikit-learn~\citep{pedregosa2011scikit}
    appear as single reference points.
    %
    \textbf{(b)}~Learning curve: holdout reward vs.\ online training steps
    ($\lambda{=}0$).  Warmup priors provide immediate quality at step~0
    (no online interactions), whereas tabula rasa starts from scratch.
    Horizontal dotted lines mark each supervised baseline's holdout reward.
    %
    Per-expert hyperparameters selected on dev-val via grid search
    (Appendix~\ref{sec:alpha_neff_ablation}); 3-model portfolio-specific
    warmup priors.
    %
    Both variants use the same train-then-freeze protocol as
    Figure~\ref{fig:prequential}: train on dev-train ($80\%$), early
    stopping on dev-val ($20\%$), final evaluation on the holdout set.
    All quality values are ground-truth multi-judge scores.}
    \label{fig:warmup_ablation}
\end{figure}
