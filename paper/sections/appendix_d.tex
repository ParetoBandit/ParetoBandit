% ============================================================================
% Appendix D: Extended Experimental Results
% Contains content demoted from main body for focus
% ============================================================================

\section{Extended Experimental Results}
\label{app:adaptation_details}

This appendix provides full experimental details, figures, and extended analysis for the adaptation and robustness results summarized in Section~\ref{sec:robustness_adaptation}.

% ----------------------------------------------------------------------------
% D.1: Catastrophic Failure Detection
% ----------------------------------------------------------------------------

\subsection{Catastrophic Failure Detection}
\label{app:catastrophic_failure}

We evaluate Corralling's response to catastrophic model failure on a \textbf{5-model portfolio} ($K{=}5$) using the production router components: \texttt{CostAwareLinUCBRouter} (warmup expert) and \texttt{CostAwareTabulaRasaRouter} (cold-start expert), coordinated via \texttt{CorrallingRouter} with importance-weighted meta-learning. Both experts explore freely across all five models---neither is hard-coded to a fixed choice---so the outcome is determined by real learning dynamics, not by construction.

\paragraph{Why $K{=}5$?} With only $K{=}2$ models, catastrophic failure detection is a trivial binary tracking problem: a simple EMA tracker just needs to learn ``model A bad, model B good'' from per-model reward averages. With $K{=}5$, post-failure routing becomes \emph{contextual}: the optimal policy must redistribute traffic across 4 remaining models based on prompt features. Each non-failing model excels in a different region of context space (via orthogonal context directions), so a per-model average tracker cannot distinguish which alternative is best for a given prompt---exactly where LinUCB's context-dependent routing provides value.

\paragraph{Warmup Priors.} Models with direct RouteLLM battle priors (Mixtral, GPT-4-Turbo) are scaled to $N_{\text{eff}}{=}10$. The remaining three models (GPT-3.5-Turbo, Claude-3-Haiku, GPT-4o) receive priors via \textbf{semantic transfer} from their nearest known neighbor, using the production system's ``First-Child Bias Correction'' (Section~\ref{sec:transfer}): preferences $\theta$ are transferred while confidence $A$ is reset to $N_{\text{eff}} \cdot I$, preserving exploration. This ensures the warmup expert has informed priors for all five models.

\begin{figure}[h]
\centering
\includegraphics[width=0.95\columnwidth]{figures/figure6_5model.png}
\caption{\textbf{Catastrophic Failure Detection: 5-Model Portfolio} ($K{=}5$, $N{=}20$ seeds, mean $\pm$ 1 std).
\textbf{(Top)}~Running-average reward. When GPT-4-Turbo fails at $t{=}100$, the Static baseline collapses to ${\sim}0.15$. banditGPT detects the failure and partially adapts (${\sim}0.65$), while the EMA Tracker reacts faster (${\sim}0.74$) by directly tracking per-model rewards. However, EMA concentrates 63\% of traffic on Mixtral alone, barely exploring GPT-3.5 (6\%) and Haiku (6\%)---missing context-dependent alternatives. The Oracle (${\sim}0.80$) achieves substantially more by routing contextually across all 4 healthy models.
\textbf{(Middle)}~Expert weight evolution shows the tabula rasa expert gaining weight during failure (up to ${\sim}0.85$), with 95\% of seeds detecting the failure.
\textbf{(Bottom)}~Model selection fractions reveal EMA's fundamental limitation at $K{=}5$: its $\epsilon/K{=}2\%$ per-model exploration budget is insufficient to evaluate all alternatives, causing it to over-concentrate on whichever model first accumulates a high EMA.}
\label{fig:decommission}
\label{fig:expert_decommission}
\label{fig:catastrophic}
\end{figure}

\paragraph{Experimental Setup.}
Five models with context-dependent rewards (orthogonal context directions per model, noise $\sigma{=}0.08$):
\begin{itemize}[nosep]
    \item Mixtral-8x7B (base 0.73), GPT-3.5-Turbo (0.70), Claude-3-Haiku (0.76), GPT-4-Turbo (0.82), GPT-4o (0.80)
    \item \textbf{Phase 1 ($t=0$--$100$):} All 5 models healthy
    \item \textbf{Phase 2 ($t=100$--$300$):} GPT-4-Turbo fails ($\mu{=}0.15$, Cohen's $d{\approx}5.0$)
    \item \textbf{Phase 3 ($t=300$--$500$):} GPT-4-Turbo recovers
\end{itemize}

Corralling settings: $\eta{=}0.3$, $\gamma{=}0.05$. Baselines: Oracle, Static GPT-4-Turbo, and EMA Tracker ($\alpha{=}0.15$, $\epsilon{=}0.1$).

\paragraph{Results ($N{=}20$ seeds).}

\begin{table}[h]
\centering
\small
\begin{tabular}{@{}lccc@{}}
\toprule
\textbf{Method} & \textbf{Healthy} & \textbf{Failure} & \textbf{Recovery} \\
\midrule
Oracle & 0.824 & 0.800 & 0.822 \\
\textbf{banditGPT} & \textbf{0.763} & \textbf{0.646} & \textbf{0.762} \\
EMA Tracker & 0.736 & 0.735 & 0.783 \\
Static GPT-4-Turbo & 0.812 & 0.153 & 0.820 \\
\bottomrule
\end{tabular}
\end{table}

\begin{itemize}[nosep]
    \item \textbf{Detection rate:} 95\% of seeds (19/20) achieve warmup weight ${<}0.15$ during Phase~2
    \item \textbf{Reaction time:} Median 34 steps (mean $49 \pm 39$, range $[0, 133]$)
    \item \textbf{Recovery rate:} 40\% of seeds detect GPT-4-Turbo recovery in Phase~3
    \item \textbf{EMA comparison:} EMA achieves $0.735$ vs.\ banditGPT's $0.646$ during failure ($\Delta{=}{-}0.090$)
\end{itemize}

\paragraph{Portfolio Size Effect.}
The gap between banditGPT and EMA narrows monotonically with portfolio size: $\Delta{=}{-}0.286$ at $K{=}2$, $\Delta{=}{-}0.154$ at $K{=}3$, and $\Delta{=}{-}0.090$ at $K{=}5$---a 69\% reduction. EMA degrades as $K$ grows because its exploration budget ($\epsilon/K$) thins across more models, while Corralling's contextual experts become more valuable as the routing decision becomes higher-dimensional. At $K{=}5$, EMA concentrates 63\% of failure-phase traffic on a single model (Mixtral), barely sampling alternatives that could be better for specific contexts.

\paragraph{Interpretation: Where Corralling Adds Value.}
The EMA tracker still outperforms banditGPT for \emph{pure model-level failure detection} because it tracks per-model rewards directly. Corralling operates one level higher, coordinating \emph{expert policies} that each maintain learned context-to-model mappings. This indirection adds detection latency but is exactly what enables prior transfer, contextual routing, and safety guarantees in the primary use case (Section~\ref{sec:pareto}). The monotonic gap closure with increasing $K$ suggests that at production-scale portfolios ($K{\geq}8$), Corralling's contextual advantage may fully compensate for its detection latency. Catastrophic failure detection remains a \textbf{secondary safety benefit} of the Corralling mechanism; for rapid model-level failure response, dedicated monitoring (e.g., EMA tracking, CUSUM) should complement Corralling.

\paragraph{Practical Implications for Production Deployments.}
Beyond the algorithmic comparison, this experiment reveals deployment-relevant differences in \emph{how} each method responds to failure:

\begin{itemize}[nosep]
    \item \textbf{Traffic concentration risk.} During the failure phase, EMA routes 63\% of traffic to a single model (Mixtral) while barely sampling GPT-3.5 (6\%) and Haiku (6\%). In production, this replaces one single point of failure (the degraded model) with another: if Mixtral subsequently hits a rate limit, experiences latency spikes, or undergoes its own quality regression, the system has no informed fallback. Corralling distributes traffic roughly uniformly across the four healthy models (${\sim}20\%$ each), providing natural load balancing and resilience against cascading failures.
    \item \textbf{Context-dependent quality.} EMA selects models based on per-model reward averages, ignoring prompt context entirely. In practice, different models excel on different task types (e.g., code generation vs.\ creative writing vs.\ structured extraction). Corralling's LinUCB experts learn these context-dependent preferences, so even during failure recovery, traffic is routed to the model most likely to perform well \emph{for each specific prompt}---not just the model with the highest average reward.
    \item \textbf{Operational monitoring architecture.} These results suggest a two-layer monitoring strategy: \textbf{(1)}~a lightweight EMA or CUSUM detector for rapid model-level alerting (triggering pages, circuit breakers, or automatic model disabling within seconds), and \textbf{(2)}~Corralling for the actual traffic redistribution, which routes contextually across the remaining portfolio rather than herding traffic to a single fallback. The two mechanisms are complementary: EMA answers ``\emph{which model failed?}'' while Corralling answers ``\emph{where should each prompt go now?}''
    \item \textbf{Portfolio growth trajectory.} Production LLM deployments are trending toward larger model portfolios as providers release specialized variants (e.g., GPT-4o-mini for speed, Claude-3.5-Sonnet for reasoning, Llama-3 for cost). The monotonic narrowing of the banditGPT--EMA gap ($\Delta$ shrinks by 69\% from $K{=}2$ to $K{=}5$) means that Corralling's value as a safety mechanism \emph{increases} along the same trajectory that production portfolios are already following.
\end{itemize}

\paragraph{Scope and Limitations.}
This experiment uses synthetic context-dependent rewards with abrupt phase transitions (Cohen's $d \approx 5.0$). Real-world model failures are typically gradual, ambiguous ($d < 0.5$), or partial (degradation on some task types only). The portfolio size scaling ($K{=}2,3,5$) uses different environment configurations, so the $\Delta$ trend should be interpreted as directional evidence rather than a precise scaling law.

% ----------------------------------------------------------------------------
% D.2: Two Operating Regimes
% ----------------------------------------------------------------------------

\subsection{Unified Framework: Two Operating Regimes}
\label{sec:three_regimes}

Our experimental program reveals that the Corralling learning rate ($\eta$) determines the adaptation regime, enabling different operational objectives. The experiments across Sections~\ref{sec:pareto}--\ref{sec:robustness_adaptation} demonstrate \textbf{complementary operating regimes}.

\begin{table}[h]
\centering
\caption{\textbf{Two Operating Regimes for Corralling-Based Routing}}
\label{tab:three_regimes}
\small
\begin{tabular}{@{}lccl@{}}
\toprule
\textbf{Regime} & \textbf{Learning Rate} & \textbf{Timescale} & \textbf{Use Case} \\
\midrule
Safety & $\eta=0.3$--$1.0$ & median $\sim$34 steps ($K{=}5$) & Catastrophic failure detection \\
Convergence & $\eta=2.0$--$5.0$ & 300--1,121 steps & Adapt beyond priors \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Timescale Separation.}
The framework reveals a safety property: catastrophic failure detection (median ${\sim}34$ steps at $K{=}5$ with $\eta{=}0.3$, 95\% detection rate) occurs faster than complete prior unlearning ($\sim$300--500 steps, $\eta{=}5.0$). However, as demonstrated in Section~\ref{app:catastrophic_failure}, dedicated monitoring mechanisms (e.g., EMA tracking) are faster for pure model-level failure detection; Corralling's primary value lies in contextual routing and prior safety, with catastrophic failure detection as a secondary benefit whose value grows with portfolio size.

\paragraph{Deployment Recommendations.}
Practitioners should select $\eta$ based on operational objectives:
\begin{itemize}[nosep]
    \item \textbf{Rapid deployment / cost focus:} $\eta=0.1$--$0.3$ (exploit priors)
    \item \textbf{Balanced cost-quality:} $\eta=1.0$ (moderate adaptation)
    \item \textbf{Quality maximization:} $\eta=2.0$--$5.0$ (complete unlearning)
    \item \textbf{Safety-critical:} $\eta=0.3$ (secondary failure detection; complement with dedicated monitoring for rapid model-level response)
\end{itemize}

% ============================================================================
% D.6: Regime-Stratified Sensitivity (Figure 8)
% ============================================================================

\subsection{Regime-Stratified Sensitivity Analysis}
\label{app:regime_stratified}

Figure~\ref{fig:expert_selection} provides the regime-stratified view of $\neff$ sensitivity referenced in Section~\ref{sec:sensitivity}.

\begin{figure}[h]
\centering
\includegraphics[width=0.95\columnwidth]{figures/figure8_regime_stratified.png}
\caption{%
\textbf{Regime-Dependent $\neff$ Sensitivity.}
\textit{Top}: Expert weights show binary switching (100\% warmup left, 100\% tabula rasa right). 
\textit{Bottom}: Performance stratified by regime reveals $\neff$ effects only when semantic transfer is used (+4.6\% warmup regime, 0\% tabula rasa). 
Overall impact: $\sim$1.5\% = $0.33 \times 4.6\%$. Robustness comes from adaptive selection, not parameter insensitivity.
}
\label{fig:expert_selection}
\label{fig:sensitivity}
\end{figure}

\paragraph{Stratified Results.}
With $N=30$ seeds, we can now estimate regime proportions with reasonable confidence:
\begin{itemize}[nosep]
    \item \textbf{Warmup-dominant regime:} Seeds where warmup expert is maintained. In this regime, $\neff=1.0$ outperforms $\neff=20.0$, confirming the over-confidence trap.
    \item \textbf{Tabula rasa-dominant regime:} Seeds where Corralling abandons warmup priors. Performance is identical across $\neff$ values, as semantic transfer is not used.
    \item \textbf{Overall:} The weighted aggregate impact across regimes is reported in Figure~\ref{fig:expert_selection}.
\end{itemize}

The increased sample size ($N=30$) enables meaningful estimation of regime proportions (see Appendix~\ref{app:multiseed_sensitivity} for exact counts and binomial confidence intervals). The conclusion that robustness emerges from adaptive expert selection (not parameter insensitivity) is supported both by the mechanism and by the regime-stratified analysis.

