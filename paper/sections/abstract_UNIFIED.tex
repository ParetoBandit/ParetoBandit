% UNIFIED ABSTRACT - Revised with corrected Figure 1 methodology
% Framing: Model preference heterogeneity, not "Alignment Tax discovery"
% Statistics: Holdout-only (N=750), unsupervised threshold, corrected effect sizes

\begin{abstract}
Large language model (LLM) inference is economically critical yet technically challenging: expensive models aren't always better, training data distributions shift, and new models release monthly. We present \textbf{banditGPT}, a production-grade contextual bandit framework that addresses all three challenges through an integrated approach combining semantic structure discovery, adaptive meta-learning (Corralling), and semantic transfer.

Our key insight is that LLM routing is a \emph{discovery} problem, not an interpolation problem: the expensive model is not merely unnecessary on some tasks---it is \emph{actively worse} on an identifiable subset. On held-out LMSYS prompts ($N=750$), ${\sim}19\%$ favor the cheaper model ($p < 0.0001$, Cohen's $d = 1.53$; confirmed with generic PCA, $d = 0.33$). When the state-of-the-art static router (RouteLLM) cannot distinguish these prompts, it pays $43\times$ more for $1.3\%$ \emph{lower} quality.

Effective routing therefore requires three mechanisms working together: (1) \textbf{semantic structure} that makes preference discovery learnable, (2) \textbf{Corralling meta-learning} that hedges against misleading training priors ($1$--$4\%$ overhead under moderate shift, with catastrophic failure protection), and (3) \textbf{semantic transfer} that eliminates cold-start penalties for new models ($+0.62$ reward, $p<10^{-7}$). Production validation demonstrates $65.9\%$ gap closure to oracle (vs $46.2\%$ for RouteLLM), with regime-dependent robustness requiring minimal tuning.

\textbf{Contribution}: None of these mechanisms alone solves the problem---the contribution is the \emph{integrated system} achieving safety guarantees, near-optimal performance, and production readiness.
\end{abstract}

% Word count: ~220 words
% Key changes from original:
% 1. Replaced "Alignment Tax" with "model preference heterogeneity"
% 2. Updated statistics: N=750 holdout-only, d=1.53, p<0.0001
% 3. Added robustness check (generic PCA d=0.33)
% 4. Removed "$2.3M/year" extrapolation (Issue #7)
% 5. Removed "17.6%" (now 19.2% with corrected threshold)
% 6. Softened claims to correlational language
