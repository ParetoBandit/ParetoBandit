# BanditGPT Dependencies
# ======================
#
# For pip-installable usage, prefer:  pip install banditgpt
# Optional extras:                    pip install banditgpt[full]
#
# This file lists ALL dependencies (core + optional) for development
# convenience.  Core dependencies match pyproject.toml [project.dependencies].

# Core (installed automatically by `pip install banditgpt`)
numpy>=1.24.0
torch>=2.0.0
sentence-transformers>=2.2.0
transformers>=4.30.0
pandas>=2.0.0

# Optional: API integration (`pip install banditgpt[full]`)
openai>=1.0.0
python-dotenv>=1.0.0

# Optional: Experiment reproduction (`pip install banditgpt[experiments]`)
matplotlib>=3.7.0
datasets>=2.14.0
