% Architecture Diagram - BanditGPT System Overview
\documentclass[tikz,border=10pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{shapes,arrows,positioning,calc,fit,backgrounds}

\begin{document}

\tikzstyle{block} = [rectangle, draw, fill=blue!20, text width=6em, text centered, rounded corners, minimum height=3em]
\tikzstyle{datablock} = [rectangle, draw, fill=green!20, text width=6em, text centered, rounded corners, minimum height=2.5em]
\tikzstyle{modelblock} = [rectangle, draw, fill=orange!20, text width=5em, text centered, rounded corners, minimum height=2em]
\tikzstyle{priorblock} = [rectangle, draw, fill=purple!20, text width=7em, text centered, rounded corners, minimum height=2.5em]
\tikzstyle{arrow} = [thick,->,>=stealth]

\begin{tikzpicture}[node distance=1.5cm, auto]

% Top: User Query
\node [datablock] (query) {User Query\\{\small "Explain..."}};

% Embedding Layer
\node [block, below=of query] (embedding) {Sentence\\Encoder\\{\small (384-dim)}};

% Bandit with Priors (central component)
\node [block, below=of embedding, fill=blue!30, minimum height=4em, text width=8em] (bandit) {LinUCB Bandit\\{\small Contextual\\Routing}};

% Shippable Priors (left side)
\node [priorblock, left=2.5cm of bandit] (priors) {Shippable\\Priors\\{\small Covariance\\Matrix <1MB}};

% Model Pool (right side)
\node [modelblock, right=1.5cm of bandit, yshift=1cm] (model1) {GPT-4o\\{\small \$4.38/1k}};
\node [modelblock, right=1.5cm of bandit] (model2) {Claude-3.5\\{\small \$3.00/1k}};
\node [modelblock, right=1.5cm of bandit, yshift=-1cm] (model3) {Gemini Flash\\{\small \$0.10/1k}};
\node [modelblock, right=1.5cm of bandit, yshift=-2cm] (modeln) {... 81 models};

% Model Pool Box
\node[draw, dashed, fit=(model1) (model2) (model3) (modeln), inner sep=0.3cm, label=above:Model Pool] (modelpool) {};

% UCB Calculation (intermediate)
\node [block, below=of bandit] (ucb) {UCB Selection\\{\small $\hat{Q} + \beta\sigma$}};

% Selected Model
\node [datablock, below=of ucb] (selected) {Selected Model\\+ Response};

% Feedback Loop
\node [block, left=of ucb, yshift=-1cm] (feedback) {Reward Signal\\{\small Quality - $\lambda$Cost}};

% Arrows - Forward Flow
\draw [arrow] (query) -- (embedding);
\draw [arrow] (embedding) -- node[right] {$\mathbf{x}_t$} (bandit);
\draw [arrow] (priors) -- node[above, text width=2cm, align=center] {\small Warm-start\\initialization} (bandit);
\draw [arrow] (bandit) -- (ucb);
\draw [arrow] (ucb) -- node[right] {$m^*$} (model1);
\draw [arrow] (ucb) -- (model2);
\draw [arrow] (ucb) -- (model3);
\draw [arrow] (model1) -- (selected);
\draw [arrow] (model2) -- (selected);
\draw [arrow] (model3) -- (selected);

% Feedback Arrow
\draw [arrow, dashed, red] (selected) -| (feedback);
\draw [arrow, dashed, red] (feedback) |- node[left, near start, text width=2cm, align=center] {\small Online\\update} (bandit);

% Annotations
\node[text width=3cm, align=left, font=\scriptsize] at (-4, -2) {
\textbf{Offline:}\\
• Distill priors from\\
\hspace{0.2cm} teacher (GPT-4o)\\
• Compress to <1MB
};

\node[text width=3cm, align=left, font=\scriptsize] at (7, -2) {
\textbf{Online:}\\
• Zero calibration\\
• Autonomous learning\\
• $O(1)$ model addition
};

\end{tikzpicture}

\end{document}

