% ============================================================================
% Appendix F: Hard Per-Request Constraint Impact
% Validates contribution (3): hard budget and latency constraint enforcement
% ============================================================================

\section{Hard Constraint Enforcement}
\label{app:constraint_enforcement}

The abstract claims hard per-request budget and latency constraints as a key contribution. This appendix provides the experimental validation of that mechanism, which operates at Layer~1 of the banditGPT architecture (Section~\ref{sec:methodology}).

\input{../experiments/appendix/F_constraint_impact/section_constraint_impact}
