\section{Complete Experimental Data (artifact pointer)}
\label{appendix:complete_data}

To keep the appendix concise while preserving full transparency, we do not reproduce the full sweep tables (including dominated points) in-line. The complete numerical results underlying Figure~\ref{fig:prequential_k2} are available in the repository artifacts:

\begin{itemize}[nosep]
    \item \path{experiments/03_figure/results/prequential_results.json}: canonical 20-seed Pareto sweep data used in the paper figures.
\end{itemize}

\noindent These files include the full cost/quality values for each evaluated operating point and are sufficient to reconstruct the plotted frontiers and reproduce any downstream analysis.

